<!DOCTYPE html>
<html lang="fr">
<head>
    <%- include('../partials/head.ejs') %>
</head>
<body>
    <h1>HELLO TWITCH !</h1>
    <ul>
        <% files.forEach(function(files) {%>
            <li><%= files.name%> | <%= files.path %> | <%= files.type%></li>
        <%});%>
    </ul>
</body>
</html>